import styled from 'styled-components'

export const Combo = styled.div`
    height:100%;
    overflow:hidden;
    display:flex;
    flex-direction:column;
    .center{
        overflow:hidden;
        display:flex;
        flex:1;
        flex-direction:column;
        img{
            width:100%;
        }
    }
`